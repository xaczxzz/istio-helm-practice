# API 라우팅 설정 요약 및 시각화

## 🏗️ 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        클라이언트 (외부)                          │
│                    localhost:80, :443, :9090                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Kind 클러스터 (로컬)                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ NodePort 매핑                                            │  │
│  │ - 31541 (HTTP) → localhost:80                           │  │
│  │ - 31026 (HTTPS) → localhost:443                         │  │
│  │ - 31090 (Monitoring) → localhost:9090 ⚠️ 미설정         │  │
│  │ - 15021 (Status) → localhost:15021                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                             │                                    │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Istio Ingress Gateway (app-gateway)                      │  │
│  │ - 포트 80 (HTTP)                                         │  │
│  │ - 포트 443 (HTTPS)                                       │  │
│  │ - 포트 9090 (Monitoring) ⚠️ 미설정                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                             │                                    │
│                             ↓                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ VirtualService (app-vs.yaml)                             │  │
│  │ 경로 기반 라우팅:                                         │  │
│  │ - /api/kiali → Kiali:20001                              │  │
│  │ - /api/grafana → Grafana:80                             │  │
│  │ - /api/jaeger → Jaeger:16686                            │  │
│  │ - /api/prometheus → Prometheus:9090                     │  │
│  │ - /api/* → API Gateway:8000                             │  │
│  │ - /* → Frontend:80                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                             │                                    │
│        ┌────────────────────┼────────────────────┐              │
│        ↓                    ↓                    ↓              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Frontend    │  │ API Gateway  │  │ Monitoring   │          │
│  │  :80         │  │ :8000        │  │ Tools        │          │
│  └──────────────┘  └──────┬───────┘  └──────────────┘          │
│                           │                                     │
│        ┌──────────────────┼──────────────────┐                 │
│        ↓                  ↓                  ↓                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │Order Service │  │Inventory Svc │  │ User Service │          │
│  │:8080         │  │:3000         │  │:8000         │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│        │                  │                  │                 │
│        └──────────────────┼──────────────────┘                 │
│                           ↓                                     │
│                  ┌──────────────────┐                           │
│                  │  PostgreSQL DB   │                           │
│                  │  :5432           │                           │
│                  └──────────────────┘                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 포트 매핑 현황

### Kind 클러스터 포트 설정

| 호스트 포트 | 컨테이너 포트 | 프로토콜 | 용도 | 상태 |
|---|---|---|---|---|
| 80 | 31541 | TCP | Istio HTTP | ✅ 정상 |
| 443 | 31026 | TCP | Istio HTTPS | ✅ 정상 |
| 8081 | 31542 | TCP | 추가 HTTP | ⚠️ 미사용 |
| 8082 | 31543 | TCP | 추가 HTTP | ⚠️ 미사용 |
| 15021 | 15021 | TCP | Istio Status | ✅ 정상 |
| 9090 | - | TCP | Monitoring | ❌ 미설정 |

### 서비스 포트 설정

| 서비스 | 포트 | 프로토콜 | 상태 |
|---|---|---|---|
| Frontend | 80 | TCP | ✅ 정상 |
| API Gateway | 8000 | TCP | ✅ 정상 |
| Order Service | 8080 | TCP | ✅ 정상 |
| Inventory Service | 3000 | TCP | ✅ 정상 |
| User Service | 8000 | TCP | ✅ 정상 |
| PostgreSQL | 5432 | TCP | ✅ 정상 |

---

## 🔄 요청 흐름 분석

### 시나리오 1: Frontend 접근

```
클라이언트
  ↓ curl http://localhost/
  ↓
Istio Ingress Gateway (포트 80)
  ↓
VirtualService (경로: /)
  ↓
Frontend Service (app-stack-frontend:80)
  ↓
Frontend Pod (nginx)
  ↓
응답: HTML 페이지
```

**상태:** ✅ 정상

---

### 시나리오 2: API 요청 (주문 조회)

```
클라이언트
  ↓ curl http://localhost/api/orders
  ↓
Istio Ingress Gateway (포트 80)
  ↓
VirtualService (경로: /api/*)
  ↓
API Gateway Service (app-stack-api-gateway:8000)
  ↓
API Gateway Pod (FastAPI)
  ↓ (내부 라우팅)
  ↓ GET /orders
  ↓
Order Service (app-stack-order-service:8080)
  ↓
Order Service Pod (Go)
  ↓
PostgreSQL (주문 데이터 조회)
  ↓
응답: JSON 주문 목록
```

**상태:** ✅ 정상 (환경 변수 올바름)

---

### 시나리오 3: 모니터링 도구 접근

```
클라이언트
  ↓ curl http://localhost:9090/api/prometheus
  ↓
Istio Ingress Gateway (포트 9090) ❌ 미설정
  ↓
실패: 연결 거부
```

**상태:** ❌ 실패 (포트 미매핑)

---

## 🔍 서비스 디스커버리

### Helm 생성 서비스 이름

```
Release: app-stack
Namespace: default

생성되는 서비스:
├── app-stack-frontend
├── app-stack-api-gateway
├── app-stack-order-service
├── app-stack-inventory-service
└── app-stack-user-service
```

### DNS 해석

```
클러스터 내부에서:
- app-stack-api-gateway.default.svc.cluster.local:8000
- app-stack-order-service.default.svc.cluster.local:8080
- app-stack-inventory-service.default.svc.cluster.local:3000
- app-stack-user-service.default.svc.cluster.local:8000
```

---

## ⚠️ 발견된 문제점

### 🔴 심각한 문제 (즉시 수정 필요)

#### 1. 모니터링 게이트웨이 포트 미매핑
- **파일:** `k8s/kind-config.yaml`
- **문제:** 포트 9090이 Kind 클러스터에 매핑되지 않음
- **영향:** 모니터링 도구 접근 불가능
- **수정:** 포트 매핑 추가

#### 2. 테스트 스크립트 포트 오류
- **파일:** `scripts/07-test-api-endpoints.sh`
- **문제:** `BASE_URL="http://localhost:8080"` (잘못된 포트)
- **영향:** 모든 테스트 실패
- **수정:** `BASE_URL="http://localhost"` (포트 80)

### 🟡 중간 수준의 문제 (개선 권장)

#### 3. VirtualService 중복
- **파일:** `frontend-virtualservice.yaml` vs `app-vs.yaml`
- **문제:** 동일 호스트에 대해 두 개의 VirtualService 정의
- **영향:** 라우팅 규칙 충돌 가능성
- **수정:** 하나로 통합

#### 4. 미사용 포트 매핑
- **파일:** `k8s/kind-config.yaml`
- **문제:** 포트 8081, 8082 정의되었으나 미사용
- **영향:** 불필요한 리소스 사용
- **수정:** 제거

### 🟢 개선 권장사항

#### 5. 타임아웃/재시도 정책 부재
- **파일:** `k8s/istio/virtual-services/app-vs.yaml`
- **문제:** VirtualService에 타임아웃/재시도 설정 없음
- **영향:** 서비스 장애 시 무한 대기 가능
- **수정:** 타임아웃 30초, 재시도 3회 추가

#### 6. Circuit Breaker 미설정
- **파일:** DestinationRule 없음
- **문제:** 장애 서비스로 인한 캐스케이딩 실패 가능
- **영향:** 시스템 안정성 저하
- **수정:** DestinationRule 추가

#### 7. 보안 정책 부재
- **파일:** NetworkPolicy 없음
- **문제:** 서비스 간 무제한 통신 허용
- **영향:** 보안 취약점
- **수정:** NetworkPolicy 추가

---

## 📋 수정 체크리스트

### 긴급 수정 (필수)

- [ ] `k8s/kind-config.yaml` - 모니터링 포트 매핑 추가
- [ ] `scripts/07-test-api-endpoints.sh` - 포트 수정 (8080 → 80)
- [ ] `k8s/istio/` - VirtualService 통합 (중복 제거)

### 권장 개선 (선택)

- [ ] `k8s/istio/virtual-services/app-vs.yaml` - 타임아웃/재시도 추가
- [ ] `k8s/istio/destination-rules.yaml` - Circuit Breaker 설정
- [ ] `k8s/network-policies.yaml` - 보안 정책 추가
- [ ] `k8s/istio/peer-authentication.yaml` - mTLS 설정

---

## 🧪 테스트 명령어

### 기본 연결 테스트

```bash
# Frontend 접근
curl http://localhost/

# API Gateway 헬스 체크
curl http://localhost/api/health

# 주문 조회
curl http://localhost/api/orders

# 사용자 조회
curl http://localhost/api/users

# 재고 조회
curl http://localhost/api/inventory
```

### 모니터링 도구 접근 (수정 후)

```bash
# Prometheus
curl http://localhost:9090/api/prometheus

# Grafana
curl http://localhost/api/grafana

# Jaeger
curl http://localhost/api/jaeger

# Kiali
curl http://localhost/api/kiali
```

### Istio 상태 확인

```bash
# VirtualService 확인
kubectl get vs -n default
kubectl describe vs app-vs -n default

# DestinationRule 확인
kubectl get dr -n default

# Ingress Gateway 확인
kubectl get svc -n istio-system | grep ingress

# 라우팅 분석
istioctl analyze
```

---

## 📈 성능 고려사항

### 현재 설정

| 항목 | 값 | 상태 |
|---|---|---|
| 타임아웃 | 미설정 | ⚠️ 기본값 사용 |
| 재시도 | 미설정 | ⚠️ 기본값 사용 |
| 연결 풀 | 미설정 | ⚠️ 기본값 사용 |
| Circuit Breaker | 미설정 | ⚠️ 미설정 |
| mTLS | 미설정 | ⚠️ 미설정 |

### 권장 설정

| 항목 | 값 | 이유 |
|---|---|---|
| 타임아웃 | 30초 | 장시간 대기 방지 |
| 재시도 | 3회 | 일시적 오류 복구 |
| 연결 풀 | 100 | 동시 연결 제한 |
| Circuit Breaker | 5 에러 후 차단 | 캐스케이딩 실패 방지 |
| mTLS | STRICT | 서비스 간 암호화 |

---

## 🎯 다음 단계

1. **긴급 수정 적용**
   - 포트 매핑 추가
   - 테스트 스크립트 수정
   - VirtualService 통합

2. **권장 개선 적용**
   - 타임아웃/재시도 정책
   - Circuit Breaker 설정
   - 보안 정책 추가

3. **검증 및 테스트**
   - 모든 엔드포인트 테스트
   - 모니터링 도구 접근 확인
   - 성능 테스트

4. **문서화 및 모니터링**
   - 라우팅 문서 업데이트
   - 모니터링 대시보드 설정
   - 알림 규칙 구성

