해결된 것:
  1. Kiali Helm alias 누락 → alias: kiali 추가
  2. Kiali 404 → VirtualService rewrite 제거
  3. Prometheus ServiceEntry 누락 → 추가
  4. Pod app 라벨 누락 → deployment template에 추가

  미해결 핵심:
  - PodMonitor가 포트 15090(제한된 메트릭)을 scrape 중 → 15020(실제 앱
  트래픽 메트릭)으로 변경해야 함
  - 로컬 파일은 수정했지만 ArgoCD selfHeal이 git 상태로 되돌리므로 git
  push가 필요

  git push 하면 전부 반영됩니다. 진행할까요?
  ⎿  User rejected Claude's plan:
     ╭──────────────────────────────────────────────────────────────────╮
     │ Kiali + Prometheus + Istio 모니터링 트러블슈팅 기록              │
     │                                                                  │
     │ 현재 환경                                                        │
     │                                                                  │
     │ - Kind 클러스터 (3 nodes)                                        │
     │ - Istio 서비스 메시 + ArgoCD GitOps                              │
     │ - 앱: frontend, order-service, user-service, inventory-service,  │
     │ postgresql                                                       │
     │ - 모니터링: Prometheus(monitoring ns), Kiali(istio-system ns),   │
     │ Grafana, Jaeger                                                  │
     │                                                                  │
     │ ---                                                              │
     │ 발견된 문제 및 수정 현황                                         │
     │                                                                  │
     │ 1. Helm Values 키 불일치 (수정 완료)                             │
     │                                                                  │
     │ - 파일: helm/monitoring/Chart.yaml                               │
     │ - 원인: dependency 이름이 kiali-server인데 values.yaml에서       │
     │ kiali: 키 사용. alias가 없어서 values가 subchart에 전달되지 않음 │
     │ - 영향: external_services.prometheus.url, auth.strategy,         │
     │ server.web_root 등 모든 Kiali 설정이 무시됨                      │
     │ - 수정: alias: kiali 추가                                        │
     │ - 상태: 수정 완료, 클러스터 반영됨 (ConfigMap 확인 완료)         │
     │                                                                  │
     │ 2. Kiali 404 에러 - VirtualService rewrite 충돌 (수정 완료)      │
     │                                                                  │
     │ - 파일: k8s/istio/unified-virtualservice.yaml                    │
     │ - 원인: VirtualService가 /monitoring/kiali/ → /kiali/로          │
     │ rewrite하는데, Kiali의 web_root은 /monitoring/kiali로 설정되어   │
     │ 경로 불일치                                                      │
     │ - 수정: rewrite 제거, /kiali/ catch-all 라우트 제거. 원래 경로   │
     │ 그대로 Kiali에 전달                                              │
     │ - 상태: 수정 완료, kubectl apply 적용됨, 200 OK 확인             │
     │                                                                  │
     │ 3. Prometheus ServiceEntry 누락 (수정 완료)                      │
     │                                                                  │
     │ - 파일: k8s/istio/monitoring-service-entries.yaml                │
     │ - 원인: Kiali, Jaeger, Grafana의 ServiceEntry는 있지만           │
     │ Prometheus ServiceEntry 없음                                     │
     │ - 수정: prometheus-external ServiceEntry 추가 (port 9090)        │
     │ - 상태: 수정 완료                                                │
     │                                                                  │
     │ 4. Pod app 라벨 누락 (수정 완료)                                 │
     │                                                                  │
     │ - 파일: 각 서비스의 helm/*/templates/deployment.yaml             │
     │ - 원인: Pod template에 app.kubernetes.io/name만 있고 Istio가     │
     │ 요구하는 app 라벨 없음                                           │
     │ - 영향: Kiali에서 "app label is missing" 경고, 워크로드 식별     │
     │ 불가                                                             │
     │ - 수정: 5개 deployment template의 pod template labels에 app: {{  │
     │ include "xxx.name" . }} 추가                                     │
     │   - helm/frontend/templates/deployment.yaml                      │
     │   - helm/order-service/templates/deployment.yaml                 │
     │   - helm/user-service/templates/deployment.yaml                  │
     │   - helm/inventory-service/templates/deployment.yaml             │
     │   - helm/api-gateway/templates/deployment.yaml                   │
     │   - helm/common/templates/deployment.yaml                        │
     │ - 주의: selector.matchLabels는 immutable이므로 변경하지 않음.    │
     │ pod template labels에만 추가                                     │
     │ - 상태: 파일 수정 완료, frontend/order/user는 반영됨,            │
     │ inventory는 git push 필요                                        │
     │                                                                  │
     │ 5. Prometheus에서 Istio 메트릭 미수집 (수정 중 - 핵심 미해결     │
     │ 이슈)                                                            │
     │                                                                  │
     │ - 파일: k8s/istio/istio-podmonitor.yaml (새로 생성)              │
     │ - 원인: kube-prometheus-stack(Prometheus Operator)은             │
     │ prometheus.io/scrape 어노테이션 기반 scrape를 하지 않음.         │
     │ PodMonitor/ServiceMonitor CRD가 필요한데 Istio용이 하나도 없었음 │
     │ - 수정: PodMonitor 생성하여 k8s/istio/kustomization.yaml에 추가  │
     │                                                                  │
     │ 포트 문제 (핵심 발견)                                            │
     │ 포트: 15090                                                      │
     │ 역할: Envoy raw stats                                            │
     │ 메트릭 내용: 제한된 메트릭만 노출. destination_app이 대부분      │
     │ unknown                                                          │
     │ ────────────────────────────────────────                         │
     │ 포트: 15020                                                      │
     │ 역할: Istio agent merged endpoint                                │
     │ 메트릭 내용: 실제 앱 트래픽 메트릭 정상 노출.                    │
     │ destination_app="order-service"                                  │
     │    등 정확한 값                                                  │
     │ - 처음 PodMonitor를 15090으로 설정 → Prometheus에                │
     │ destination_app="unknown"만 수집됨                               │
     │ - 15020으로 변경 필요                                            │
     │                                                                  │
     │ 현재 상태 & 남은 문제                                            │
     │                                                                  │
     │ - PodMonitor를 15020으로 수정하여 kubectl apply 했으나, ArgoCD   │
     │ selfHeal이 이전 설정(15090)으로 되돌림                           │
     │ - ArgoCD istio-config 앱이 k8s/istio/ 디렉토리를 관리하고 있어,  │
     │ git push 없이는 변경이 유지되지 않음                             │
     │ - 모니터링 namespace의 서비스들(grafana, alertmanager 등)의      │
     │ 트래픽은 잘 수집됨 - 이들은 15090에서도 metrics가 정상 노출되기  │
     │ 때문                                                             │
     │ - 앱 서비스(order-service, user-service 등)의 트래픽이 수집 안   │
     │ 됨 - 15020 포트에서만 정상 노출                                  │
     │                                                                  │
     │ ---                                                              │
     │ 수정이 필요한 파일 목록 (git push 필요)                          │
     │ 파일: helm/monitoring/Chart.yaml                                 │
     │ 변경 내용: kiali-server에 alias: kiali 추가                      │
     │ 상태: 로컬 수정 완료                                             │
     │ ────────────────────────────────────────                         │
     │ 파일: k8s/istio/unified-virtualservice.yaml                      │
     │ 변경 내용: Kiali rewrite 제거                                    │
     │ 상태: 로컬 수정 완료                                             │
     │ ────────────────────────────────────────                         │
     │ 파일: k8s/istio/monitoring-service-entries.yaml                  │
     │ 변경 내용: Prometheus ServiceEntry 추가                          │
     │ 상태: 로컬 수정 완료                                             │
     │ ────────────────────────────────────────                         │
     │ 파일: helm/*/templates/deployment.yaml (6개)                     │
     │ 변경 내용: Pod에 app 라벨 추가                                   │
     │ 상태: 로컬 수정 완료                                             │
     │ ────────────────────────────────────────                         │
     │ 파일: k8s/istio/istio-podmonitor.yaml                            │
     │ 변경 내용: targetPort: 15020으로 변경 필요                       │
     │ 상태: 로컬 수정 완료                                             │
     │ ────────────────────────────────────────                         │
     │ 파일: k8s/istio/kustomization.yaml                               │
     │ 변경 내용: istio-podmonitor.yaml 참조 추가                       │
     │ 상태: 로컬 수정 완료                                             │
     │ ---                                                              │
     │ 다음 단계                                                        │
     │                                                                  │
     │ 1. istio-podmonitor.yaml의 targetPort가 15020인지 최종 확인      │
     │ 2. 모든 변경사항 git commit & push                               │
     │ 3. ArgoCD sync 후 PodMonitor가 15020으로 적용되었는지 확인       │
     │ 4. 앱에 트래픽 발생 (브라우저 접속 또는 load test)               │
     │ 5. Kiali Traffic Graph에서 서비스 간 통신 확인                   │
     │                                                                  │
     │ 검증 명령어                                                      │
     │                                                                  │
     │ # PodMonitor 포트 확인                                           │
     │ kubectl get podmonitor -n monitoring -o                          │
     │ jsonpath='{.items[*].spec.podMetricsEndpoints[*].targetPort}'    │
     │                                                                  │
     │ # Prometheus target 확인 (15020 포트인지)                        │
     │ # Prometheus UI → Status → Targets에서 istio-proxy-metrics job   │
     │ 확인                                                             │
     │                                                                  │
     │ # 메트릭 수집 확인                                               │
     │ kubectl exec -n <pod> -- curl -s                                 │
     │ 'http://prometheus:9090/api/v1/query' \                          │
     │   -G --data-urlencode 'query=count by (destination_app)(istio_re │
     │ quests_total{destination_app!="unknown"})'                       │
     │                                                                  │
     │ # Pod app 라벨 확인                                              │
     │ kubectl get pods -n default -o                                   │
     │ custom-columns='NAME:.metadata.name,APP:.metadata.labels.app'    │
     ╰──────────────────────────────────────────────────────────────────╯