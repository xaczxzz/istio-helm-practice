replicaCount: 2

image:
  repository: kind-registry:5000/user-service
  pullPolicy: IfNotPresent
  tag: "v2"

service:
  type: ClusterIP
  port: 8000

serviceAccount:
  create: false
  automount: true
  annotations: {}
  name: ""

resources:
  requests:
    memory: "64Mi"
    cpu: "50m"
  limits:
    memory: "128Mi"
    cpu: "200m"

autoscaling:
  enabled: false

env: {}